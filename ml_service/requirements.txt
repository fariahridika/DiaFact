# Pinned where the published experiment cares; dice-ml 0.12 needed for
# pandas 2.x (0.11 required pandas<2 and failed pip resolve on Render/Linux).
xgboost==3.1.3
# 0.50+ required for xgboost>=3.1 (base_score is "[0.55]" not a bare float).
shap==0.50.0
dice-ml==0.12
scikit-learn==1.5.1
imbalanced-learn==0.12.3
pandas==2.2.2
numpy==1.26.4
flask==3.0.3
flask-cors==4.0.1
joblib==1.4.2
gunicorn==22.0.0 ; sys_platform != "win32"
waitress==3.0.0  ; sys_platform == "win32"
